% ============================ RELATED WORK ============================
% Structure: manual + three autonomy families (geometry, hierarchical, RL).
% See marv research/baselines/baselines_todo.md.
Flipper control ranges from manual teleoperation to three families of autonomy:
geometry-based planning, hierarchical switching, and reinforcement learning.
Table~\ref{tab:sensing} summarizes what each method must sense.

\subsection{Manual and mode-based teleoperation}
In direct teleoperation the operator drives the flippers by hand~\cite{cihala2025}, or
switches between preset flipper modes~\cite{larochelle2013,azayev2022}. Manual control
needs no map: the operator observes the terrain and closes the control loop by hand.

\subsection{Geometry-based / model-based planning}
A family that plans flipper motion from an explicit terrain model, and is the
\emph{most} map-dependent of all. Chen et al. combine dynamic programming with a
fast geometry-based pose predictor over the elevation
point cloud~\cite{chen2023}; Yuan et al. plan in the flipper configuration space
from known stair geometry with motion-capture ground truth~\cite{yuan2019}; Oehler
and von Stryk predict pose on a signed-distance field built from
LiDAR~\cite{oehler2024}; and Xu et al. solve a receding-horizon hybrid trajectory
optimization over a simplified terrain sampled from an a priori 3D occupancy map with
FAST-LIO2 odometry~\cite{xu2024}; and, most recently, Zhang et al. plan the path and flipper
maneuvers with RRT* over a contact-model pose predictor, using learned semantic
segmentation of the elevation map only as a perception front-end~\cite{zhang2025}.

\subsection{Hierarchical / switching / mixture-of-experts control}
A family that composes low-level behaviors under a selector. The adaptive-traversability line~\cite{zimmermann2014} classifies
terrain into discrete flipper modes from an elevation map, and for the selected mode a
simple low-level controller drives the flippers. A learned state machine~\cite{azayev2022}
switches among seven hand-crafted flipper templates selected from an elevation map. Each
state overlays a \emph{roll}-stabilization term and a stagnation-triggered \emph{escape
maneuver} that lowers all four flippers to free the robot. It is the one prior method
that couples anti-stuck recovery with attitude stabilization, and the origin of the
recovery we adopt.

\subsection{Monolithic model-free deep RL}
FTR-Bench standardizes this setting, benchmarking SAC, PPO, TD3, DDPG, and TRPO on a
flipper-track robot in Isaac Lab~\cite{ftrbench2025}. Pecka et al. learn a
safety-constrained policy (Constrained REPS) whose state is body pitch plus the terrain
height read from an online octomap~\cite{pecka2016}. Discrete-action variants dominate the
flipper-specific literature: Pan et al. learn a dueling double-DQN over discrete
front/rear flipper increments~\cite{pan2023atd3qn} and extend it with an
intrinsic-curiosity module~\cite{pan2023icm}; both take a local elevation map,
downsampled from a LiDAR point-cloud map built with A-LOAM~\cite{loam2014,aloam}, plus
chassis pitch. Their reward rewards smooth pitch only, and getting stuck is a terminal
penalty. Mitriakov et al.
learn staircase negotiation with a policy-gradient method, first from step-edge
geometry with marker-based ground truth~\cite{mitriakov2021} and then in an
open-source Gazebo/ROS framework from depth-image features with ground-truth
pose~\cite{mitriakov2022}, injecting stability through center-of-gravity and
pitch-rate reward shaping. Most recently, C-TRAC learns a contact-aware latent from noisy
LiDAR and drives an asymmetric-SAC policy that, unusually, stabilizes both roll and pitch
via a normalized-energy-stability-margin reward~\cite{ctrac2025}.

\subsection{Attitude stabilization and mapless posture control}\label{sec:related-posture}
Flipper-based attitude stabilization has been studied before, but nearly always on top of
an elevation map: a roll-only stabilization term inside a map-based state
machine~\cite{azayev2022}, and C-TRAC levelling both roll and pitch from a reward computed
on the map-based simulator state~\cite{ctrac2025}. Rocha et al.~\cite{rocha2023} regulate
roll, pitch, and ground clearance of an articulated tracked vehicle from the IMU and the
flipper joints alone, but the method is not designed for obstacle traversal. It needs no
exteroceptive sensor and no map, and it is the closest prior work to ours in what it
senses.

\subsection{Evaluating traversal quality}
We evaluate a controller by the quality of the traversal it produces, measured
on the robot's body: IMU-based smoothness such as pitch swing and vertical
shock~\cite{chen2023}, body attitude, and chassis-to-ground clearance.
Following~\cite{cihala2025,zimmermann2014} we score shock, tilt, and clearance,
and we compute all three against ground truth. In simulation the reference
comes from the simulator; on the real robot, from external localization and a
handcrafted map of each obstacle.

\begin{table*}[t]
\centering
\caption{\textbf{List of compared baselines.} \emph{Family}: baseline family
(Sec.~\ref{sec:related}). \emph{Approach}: how the flipper command is produced.
\emph{Code}: whether we run the authors' released implementation or our own
reimplementation from the paper. \emph{Map \& loc.}: needs a robot-centric elevation map
and the localization that registers it beneath the moving chassis (built onboard, it
drifts with odometry). \emph{Stabilizes}: attitude axes with an explicit stabilization
objective (r roll, p pitch). \emph{Anti-stuck}: frees itself when stuck. Only manual
teleoperation and \SAS{} need no map; \SAS{} alone stabilizes both attitude axes and
recovers when stuck. \TODO{confirm which Mitriakov variant we actually ran.}}
\label{tab:sensing}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lllcccc@{}}
\toprule
Method & Family & Approach & Code & Map \& loc. & Stabilizes & Anti-stuck \\
\midrule
Manual control~\cite{cihala2025}   & manual   & direct teleoperation            & --        & \xmark & --   & \xmark \\
Manual modes~\cite{azayev2022}     & manual   & operator switches preset modes  & --        & \xmark & --   & \xmark \\
{\v C}\'ihala \GC/\OA~\cite{cihala2025} & react./geom. & clearance + obstacle-aware controllers & released & \cmark & -- & \xmark \\
Chen GFMP~\cite{chen2023}          & geometry & pose prediction + dynamic prog. & reimpl.   & \cmark & p    & \xmark \\
Yuan~\cite{yuan2019}               & geometry & configuration-space planning    & reimpl.   & \cmark & --   & \xmark \\
Oehler~\cite{oehler2024}           & geometry & pose prediction on an SDF + margin search & released & \cmark & -- & \xmark \\
Xu HTO~\cite{xu2024}               & geometry & hybrid trajectory optimization  & reimpl.   & \cmark & p    & \xmark \\
Zhang~\cite{zhang2025}             & geometry & contact-model pose prediction   & reimpl.   & \cmark & --   & \xmark \\
Azayev~\cite{azayev2022}           & hier./IL & learned state machine over templates & released & \cmark & r & \cmark \\
Pecka~\cite{pecka2016}             & RL       & safety-constrained REPS         & reimpl.   & \cmark & p    & \xmark \\
AT-D3QN~\cite{pan2023atd3qn}       & RL       & dueling double-DQN              & reimpl.   & \cmark & p    & \xmark \\
ICM-D3QN~\cite{pan2023icm}         & RL       & D3QN + intrinsic curiosity      & reimpl.   & \cmark & p    & \xmark \\
Mitriakov~\cite{mitriakov2021}     & RL       & policy-gradient stair climbing  & released  & \cmark & p    & \xmark \\
Mitriakov~\cite{mitriakov2022}     & RL       & policy-gradient stair climbing  & released  & \cmark & p    & \xmark \\
C-TRAC~\cite{ctrac2025}            & RL       & contact-aware asymmetric SAC    & reimpl.   & \cmark & r\&p & \xmark \\
\midrule
\textbf{\SAS{} (ours)}             & reactive & attitude stabilization + anti-stuck & ours & \xmark & r\&p & \cmark \\
\bottomrule
\end{tabular}
\end{table*}

